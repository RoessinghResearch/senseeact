package nl.rrd.senseeact.client.model.compat;

/**
 * The supported sensor types. Each type should have one or more {@link
 * SensorProduct SensorProduct}s for that type.
 *
 * @author Dennis Hofs (RRD)
 */
public enum SensorTypeCompat {
	ACTIVITY,
	GOAL_PROJECT,
	WEIGHT
}
